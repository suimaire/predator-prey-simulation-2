// Standalone SQL Editor entry points, generated from one reviewed contract.
import { readFileSync, writeFileSync } from 'node:fs';
const read = name => readFileSync(new URL(`../${name}`, import.meta.url), 'utf8').replace(/\r\n/g, '\n');
const header = `-- GENERATED by npm run db:generate; edit supabase/leaderboard-v2/ sources.\n-- Production requires explicit approval and runbook preflight.\nbegin;\nset local lock_timeout = '5s';\nset local statement_timeout = '120s';\n`;
const lock = `lock table public.apex_leaderboard in access exclusive mode;\n`;
const common = read('supabase/leaderboard-v2/contract.sql');
const finalize = read('supabase/leaderboard-v2/close-legacy.sql');
const columns = `alter table public.apex_leaderboard add column if not exists school_name text;\nalter table public.apex_leaderboard add column if not exists school_key text;\n`;
const footer = `\nnotify pgrst, 'reload schema';\ncommit;\n`;
const outputs = {
  'supabase/schema.sql': header + read('supabase/leaderboard-v2/install.sql') + lock + common + finalize + footer,
  'supabase/migrations/20260922_leaderboard_schools_v2.sql': header + lock + columns + common + footer,
  'supabase/manual-cutover/20260922_finalize_leaderboard_v2.sql': header + lock + finalize + footer,
};
for (const [path, body] of Object.entries(outputs)) {
  if (process.argv.includes('--check')) {
    if (read(path) !== body) throw new Error(`${path} is stale; run npm run db:generate`);
  } else writeFileSync(new URL(`../${path}`, import.meta.url), body);
}
